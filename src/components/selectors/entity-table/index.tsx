type EntityTableProps = {
  searchGlobalField?: string;
  sortGlobalField?: string;
  paginationGlobalField?: string;
  actions: {
    schema: string;
  }[];
};

export function EntityTable(props: EntityTableProps) {
  throw "unsupported";
}
